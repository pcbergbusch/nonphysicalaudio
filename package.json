{
  "name": "nonphysicalaudio",
  "version": "0.0.1",
  "repository": {
    "url": "git+https://github.com/pcbergbusch/nonphysicalaudio.git"
  },
  "description": "Website for Nonphysical Audio",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean_windows": "rd /s /q node_modules && rd /s /q build && del package-lock.json",
    "clean_linux": "rm -rf node_modules && rm -rf build && rm -f package-lock.json"
  },
  "author": "Paul Bergbusch",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^18.2.8",
    "@types/react-dom": "^18.2.4",
    "gh-pages": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "sass": "^1.62.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
